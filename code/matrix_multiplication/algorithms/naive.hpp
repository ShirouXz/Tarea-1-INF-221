#ifndef NAIVE_H
#define NAIVE_H

#include <vector>

//Declaracion de la funcion naive para la multiplicacion de matrices
std::vector<std::vector<int>> naive(const std::vector<std::vector<int>> &mat1, const std::vector<std::vector<int>> &mat2);

#endif